# 更新日志

按 Keep a Changelog 的思路编写,留个记录,方便知道每次更新都改了什么,也方便用户查看更新内容.

## [Unreleased]
之后规划的功能和修复会记录在这里,等到正式发布后再移到下面对应的版本里（画饼.

## 2.0.6.1-[Cyrene] - 2026-05-02

### Added
- UP 主空间页和关注列表新增自定义备注功能，支持在空间页三点菜单或备注区点击编辑，数据持久化本地存储，清空保存则删除备注。
- 过滤列表编辑体验增强：关键词列表支持就地点击编辑（失焦或回车保存），用户屏蔽列表改为可长按复制的可选文本，操作更直观。
- （上游改动）直播全屏模式下的 SuperChat 展示宽度支持自定义调节。（#1910）

### Changed
- （上游改动）SuperChat 结束时间计算逻辑优化，显示更准确。
- （上游改动）浮动列表头部（SliverFloatingHeader）行为修复与优化。（#1917）

### Fixed
- 修复 iOS 从小窗（PiP）返回全屏后播放器意外暂停的问题（surface 脱离为异步操作，将状态恢复延迟至下一帧执行，并正确保留小窗期间的播放意图）。
- 修复 Android 重启应用后应用内音量未恢复至上次设置值的问题。
- （上游改动）修复 Android 端手动调低亮度后亮度被异常重置的问题。
- （上游改动）更新 iOS BottomSheet 补丁，修复相关崩溃。（#1929）
- （社区贡献）修复播放器横竖屏方向切换相关问题。（#1935）

## 2.0.5.2-[Cifera] - 2026-04-30

### Changed
- （上游改动）**重新启用**图片预览时自动隐藏系统状态栏与导航栏（上个版本因稳定性回退，此次修复后重新上线）。
- （上游改动）收藏夹排序、关注分组排序、底栏按钮排序的拖拽交互逻辑统一优化，体验更流畅。
- （上游改动）直播间消息列表在需要时自动刷新，减少后台切回后弹幕断流的情况。
- （上游改动）折叠屏尺寸处理优化，展开/折叠状态下的界面适配更准确。
- （上游改动）直播 SuperChat 卡片、动态、收藏夹等多处 UI 细节微调。
- （上游改动）iOS 平台 BottomSheet 弹出行为修复。（#1906）

### Fixed
- 修复直播间弹幕列表自动滚动异常（滚动位置跳动），以及从后台切回后弹幕停止更新的问题。
- 修复多账号场景下跨视频跳转时错误恢复至上一条视频旧进度的问题。
- （上游改动）改善 Android 小窗与分屏模式下的窗口处理逻辑。（#1908）

## 2.0.5.1-[Castorice] - 2026-04-29

### Added
- 离线缓存视频新增 CC 字幕下载与离线播放支持，下载时自动抓取字幕文件，播放本地缓存时可正常显示字幕。（#84）
- 播放器新增应用内音量控制功能，支持在不调整系统音量的前提下独立控制应用音量。（#85）
- 评论区过滤功能增强：新增按用户 ID 屏蔽和按账号等级过滤，帮助更精准地屏蔽低质内容。（#82）
- （上游改动）关注分组新增自定义排序支持，可手动调整分组顺序。
- （上游改动）创建收藏夹或关注分组后自动将当前视频加入，减少重复操作。
- （上游改动）动态投票卡片支持查看投票配图。
- （上游改动）用户空间搜索切换为 gRPC 接口，数据更完整，支持更多视频信息展示。
- （社区贡献）评论列表新增"添加到关键词过滤"快捷入口，可直接从评论操作菜单将内容加入过滤列表。（#1903）

### Changed
- （上游改动）视频卡片新增视频尺寸预检，可提前识别竖版/横版视频并优化卡片布局，适用于历史、收藏、搜索等多处列表。
- （上游改动）动态/话题页的可拖动底部面板重构优化，拆分为独立组件，交互更流畅。
- （上游改动）文本选择交互优化，移除冗余的自定义 Flutter 实现，改用 SDK 原生支持，减少维护成本。
- （上游改动）音频响度归一化处理逻辑优化，提升不同视频间的音量一致性体验。
- （上游改动）横向视频卡片数据模型统一，减少冗余定义，提升一致性。
- （上游改动）图片浏览手势交互优化。
- （上游改动）视频页退出/返回处理逻辑优化。
- （上游改动）投稿面板改为实时获取视频时长，显示更准确。
- （上游改动）**撤回**上个版本"图片预览时自动隐藏系统栏"功能（该功能存在稳定性问题）。
- （社区贡献）定时关闭功能在退出前自动同步当前视频播放进度，避免关播记录丢失。（#1896）

### Fixed
- 修复应用内小窗（PiP）返回主界面时视频自动暂停的问题。
- 修复小窗模式返回后无法切换"仅音频"模式的问题。（#83）
- 修复离线缓存视频在开启弹幕合并时弹幕不显示的问题。（#91）
- 修复音量增强功能二次确认手势判断不准确的问题。
- 修复评论区关键词过滤在上游存储格式变更后失效的问题（新旧格式兼容）。
- 修复 Opus 专栏正文字体过小的问题（新格式用 font_level 字符串替代了数字 font_size，导致回退到默认字号）。（#1897）
- 修复使用 AAudio 输出时系统压低音量（如来电通知）触发应用内音量指示器误弹的问题。（#1899）
- （上游改动）修复收藏夹排序操作不生效的问题。
- （上游改动）修复图片手势识别异常的问题。
- （上游改动）修复播放器控制器相关问题。（#1904）
- （上游改动）修复 SVG 图片加载失败时错误展示逻辑不正确的问题。
- （上游改动）iOS 上禁用屏幕亮度自动重置行为，避免播放后亮度异常。

## 2.0.4.1-[SliverWolf] - 2026-04-20

### Added
- 直播超级聊天（SC）卡片新增发送时间显示，可以在播放器设置中设置（#81）
- 新增评论区点赞/点踩按钮位置交换选项，现在可在设置中调整布局顺序。（#79）
- （上游改动）评论区新增翻译按钮，当服务端返回 showTranslate 时显示，支持在线翻译评论内容。
- （上游改动）Linux 平台新增 Geetest 极验登录支持，通过 linux_webview_window 实现验证码界面。（#1889）

### Changed
- 点击底栏首页或动态 Tab 触发刷新时，新增固定位置的刷新指示器，不再随列表滚动位置变化而跳动。（#80）
- （上游改动）图片查看器全屏预览时自动隐藏系统状态栏与导航栏，提升沉浸感。
- （上游改动）播放器新增"移除安全边距"选项，允许视频画面延伸至系统栏区域。
- （上游改动）Android 横竖屏切换逻辑进一步优化，提升稳定性。
- （上游改动）滚动视图交互行为优化与修正。
- （上游改动）头像悬挂组件（pendant avatar）点击区域与渲染逻辑优化。
- （上游改动）设置页数据模型拆分重构（extra_settings / style_settings / model），SwitchItem 组件同步调整，便于后续维护扩展。
- （上游改动）直播间页面结构调整，将部分控制逻辑从 Controller 移入 View 层。

### Fixed
- （上游改动）修复 iOS 切换网络时 Dio 适配器未重置导致请求失败的问题。（#1885）
- （上游改动）修复桌面端 RichTextField 文本光标 affinity 判断错误的问题。
- （上游改动）修复弹幕 proto 字段编号不足导致扩展字段解析失败的问题。
- （上游改动）修复请求重试时账号管理器判断逻辑错误。
- （上游改动）修复竖向拖动手势识别器 isDyAllowed 初始值错误的问题。
- （上游改动）修复 iOS 构建问题。

## 2.0.3.1-[Nicole] - 2026-04-13

### Added
- 离线缓存播放新增元数据持久化，播放本地缓存时可完整恢复章节进度条与 SponsorBlock
- 弹幕合并设置新增"代表性时间点"百分位配置（默认20%），合并后弹幕时间戳的决定将基于更具代表性的时刻，而不再固定取最早一条，对话场景下效果更自然，不会出现提前出现的情况。
- 首页 App 端推荐视频卡片新增充电专属角标，方便快速识别充电视频。（#68）
- 倍速纳入单独视频设置，切换视频或复用播放器时自动重置为默认倍速，避免上一条视频的临时倍速影响下一条。
- 动态页关键词屏蔽新增快捷添加入口，可直接从视频菜单将相关词加入屏蔽列表。
- 检查更新新增"检测预发布版本更新"选项（默认关闭），开启后自动检查时将检测 pre-release 版本更新；"不再提醒"改为按版本号跳过，不再全局关闭自动检查，下个版本发布后仍会正常提示。
- （上游改动）新增 Bubble 页（大会员泡泡页），支持展示泡泡动态、分类浏览及相关 URL Scheme 跳转，并在历史记录、个人页等多处补充入口。
- （上游改动）Android 新增桌面快捷方式，支持从桌面长按应用图标快速跳转到下载或搜索。

### Changed
- 章节分割点在进度条上的视觉效果进一步优化，底部控制栏按钮顺序同步微调，视觉层次更清晰。
- （上游改动）进度条拖动交互行为优化。（#1887）
- （上游改动）视频页状态栏显示逻辑优化。
- （上游改动）重构 Android 屏幕方向处理逻辑，横竖屏切换更稳定，这个有反馈说效果不好，更新后麻烦多试几次看看还有没有问题，有问题我之前有写一套基于原本2.0.2版本基础优化的方案可以回退。

### Fixed
- 修复 PopupMenuButton 在预测性返回手势激活期间偶发崩溃的问题。
- 修复多账号配置下番剧缓存误报"需要大会员"的问题。
- 修复多账号配置下"回到上次观看位置"在番剧上失效的问题。（关闭 #66）
- 修复跨视频复用播放器时未重置临时播放器设置（倍速、画面比例等）的问题，避免上一条视频的临时设置意外延续到下一条。
- 调整缓存详情界面画质标注文本为（"目标画质"）。
- （上游改动）修复部分场景下返回按钮失效的问题。（#1883）

## 2.0.2.2-Linnea - 2026-04-08

### Added
- 离线缓存新增“全部视频 / 文件夹”双视图，支持本地文件夹管理、多对多归类、批量添加到文件夹，以及按当前文件夹顺序播放与连播，解决缓存内容越来越多后难以整理和查找的问题。
- 离线缓存支持手动排序，并补齐文件夹排序能力，让用户可以按照自己的观看习惯组织缓存内容，而不再只能依赖默认顺序。
- 下载合集内容时会自动创建并复用对应文件夹，避免同一合集的缓存视频散落在总列表里，降低后续整理成本。
- 动态流与推荐流新增共享白名单，并支持在视频菜单和作者面板中一键加白，减少用户反复屏蔽或放行同一位 UP 主的操作成本。
- Android 新增预测性返回支持（须设置过渡动画为Native），并提供开关项，让系统返回预览动画与应用页面切换更加一致。
- “我的”页面新增类似收藏区的历史记录卡片预览，方便用户快速继续最近看过的内容。
- 搜索结果列表新增“已点击视频标题变灰”效果，帮助用户快速区分已看与未看的搜索结果。
- Android 更新体验优化，支持自动识别设备架构并匹配更合适的 APK 下载入口，更新不再需要手动选择下载。
- （上游改动）用户主页新增更多空间页能力，包括预约按钮、舰长信息展示、舰长榜入口，以及 Android 用户快捷方式支持，方便高频访问常看的 UP 主。

### Changed
- 离线缓存卡片的删除、更新弹幕、添加到文件夹等操作统一移入右下角菜单，让长按专门用于进入多选，交互方式与收藏、稍后再看更一致。
- 快速收藏相关提示文案进行了调整，更加直观。
- 视频菜单、动态作者面板以及“允许与其他应用同时播放”等部分文案与操作顺序进行了微调，减少误解并提升可发现性。
- 章节进度条显示方式进一步优化，支持更直观地展示当前章节名称与章节分割点，降低用户在长视频中定位章节的成本。
- 升级了 GetX 相关实现，替换为自己的Fork版本，为预测性返回、提供底层支持。

### Fixed
- 修复离线缓存播放从小窗展开时出现灰屏、无响应，提升 PiP 与缓存播放之间的切换稳定性。
- 修复“从小窗展开后进入听视频再返回”时播放器消失的问题，避免播放器状态恢复不完整导致页面空白。
- 修复多账号配置下无法正确恢复上次观看进度的问题，避免用户被错误定位到开头或无法继续上次播放位置。
- 修复 API 签名中空格编码不一致导致的搜索 `API key error`，提高搜索请求的兼容性与稳定性。
- 修复预测性返回相关的界面闪烁与视频页顶部白线问题，让返回动画与页面渲染更加平滑。
